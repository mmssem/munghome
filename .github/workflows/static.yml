name: munghome2

on:
  push:
    branches: [ "main" ]

jobs:
  laravel-tests:
    runs-on: self-hosted
    env:
      USER_NAME : mungkhs
      SOURCE_DIR : munghome
    steps:
    - uses: actions/checkout@v3
    - name : Install Dependencies
      run : composer install
    - name : Copy .env
      run : cp ./../.env ./
    - name : Directory Permissions
      run : sudo chmod -R 777 bootstrap/cache && sudo chown -R ${{env.USER_NAME}}:www-data public storage && sudo chmod -R 774 storage
    - name : faker_ko_KR Lorem.php copy
      run : cp ./../Lorem.php ./vendor/fakerphp/faker/src/Faker/Provider/ko_KR/
